const Collection = require('@Collection');
const { ObjectId } = Collection.Types;

module.exports = new Collection({
    name: 'testing_steps',
    symbol: 'TSTP',
    displayName: '',
    pluralLabel: '',
    singularLabel: '',
    fieldsSet: []
});
