const Create = require('./Create');
const UpdateDocument = require('./UpdateDocument');
const GetQueryCollection = require('./GetQueryCollection');
const GetDoc = require('./GetDoc');
const Delete = require('./Delete');

module.exports = {
    Create,
    UpdateDocument,
    GetQueryCollection,
    GetDoc,
    Delete
};
